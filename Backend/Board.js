class Board {
    constructor(x, y) {
      // Use these to check boundries
      this.x = x;
      this.y = y;
  
      this.board = 
      
      // entities
      this.entities = []
  
    }
  
    update_tick(){
      console.log()
      // Make the call to the clients
      fetch()
    }
  }